export enum API_PATH {
  GET_ORDERS = 'order-get',
  PAY_ORDERS = 'order-pay',
}

export enum ORDER_STATUS {
  New = 0,
  InProgress = 1,
  Ready = 2,
}

export enum STRINGS {
  DEFAULT_RESTAURANT_NAME = 'Zum Grundstein',
}
